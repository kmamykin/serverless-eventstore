{
  "name": "eventstore",
  "version": "0.0.1",
  "description": "A Serverless Event Store",
  "author": "Kliment Mamykin<kmamyk@gmail.com>",
  "license": "MIT",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git://github.com/"
  },
  "scripts": {
    "deploy:init": "serverless project init --debug -s $STAGE -r $REGION -p $AWS_PROFILE",
    "deploy": "npm run deploy:resources && npm run deploy:functions && npm run deploy:endpoints",
    "deploy:resources": "serverless resources deploy --debug -s $STAGE -r $REGION",
    "deploy:functions": "serverless function deploy --debug -s $STAGE -r $REGION -a",
    "deploy:endpoints": "serverless endpoint deploy --debug -s $STAGE -r $REGION",
    "test": "echo PASSED!"
  },
  "dependencies": {},
  "devDependencies": {
    "serverless": "^0.5.1"
  }
}